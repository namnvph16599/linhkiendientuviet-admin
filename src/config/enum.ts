export enum NftType {
  METABORG = 'metaborg',
  LAND = 'land',
  PART = 'part',
  ITEM = 'item',
  BOX = 'box'
}

export enum ModalStatus {
  PREPARE = 0,
  REQUIRED_LOADING = 1,
  UNREQUIRED_LOADING = 2,
  SUCCESS = 3,
  ERROR = 4
}

export enum ConfirmModalType {
  WARNING = 'warning',
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info'
}
